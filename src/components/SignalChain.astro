<section class="section signal-section" id="workflow">
  <div class="container">
    <div class="section-header reveal">
      <p class="section-label">Workflow</p>
      <h2>Source to <code>_DG</code> in five steps</h2>
      <p class="lead">
        Analyze your library, review per-track numbers, then render copies. Library Tuning is optional profiling along the way.
      </p>
    </div>

    <div class="signal-chain reveal" aria-label="DropGain processing workflow">
      <div class="signal-track">
        <div class="signal-grid">
          <div class="signal-node" data-col="1">
            <span class="signal-dot" aria-hidden="true"></span>
            <span class="signal-label">Source folder</span>
          </div>
          <span class="signal-connector" data-col="2" aria-hidden="true"></span>

          <div class="signal-node" data-col="3">
            <span class="signal-dot" aria-hidden="true"></span>
            <span class="signal-label">Analyze</span>
          </div>
          <span class="signal-connector" data-col="4" aria-hidden="true"></span>

          <div class="signal-node" data-col="5">
            <span class="signal-dot" aria-hidden="true"></span>
            <span class="signal-label">Review table / waveform</span>
          </div>
          <span class="signal-connector" data-col="6" aria-hidden="true"></span>

          <div class="signal-node" data-col="7">
            <span class="signal-dot" aria-hidden="true"></span>
            <span class="signal-label">Render</span>
          </div>
          <span class="signal-connector" data-col="8" aria-hidden="true"></span>

          <div class="signal-node signal-node-mono" data-col="9">
            <span class="signal-dot" aria-hidden="true"></span>
            <span class="signal-label">*_DG outputs</span>
          </div>

          <div class="signal-branch">
            <span class="signal-branch-line" aria-hidden="true"></span>
            <div class="signal-node signal-node-branch">
              <span class="signal-dot signal-dot-branch" aria-hidden="true"></span>
              <span class="signal-label">Library Tuning</span>
              <span class="signal-sublabel">optional</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .signal-section {
    background: var(--bg-panel);
    border-block: 1px solid var(--border);
  }

  .signal-chain code {
    font-size: 0.9em;
    color: var(--accent);
  }

  .signal-track {
    overflow-x: auto;
    padding-bottom: var(--space-2);
    -webkit-overflow-scrolling: touch;
  }

  .signal-grid {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-4);
    width: 100%;
  }

  .signal-node {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-2);
    padding: var(--space-4);
    min-width: 7rem;
    max-width: 9rem;
    text-align: center;
  }

  .signal-dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: var(--accent);
    box-shadow: 0 0 12px rgb(176 220 232 / 50%);
    flex-shrink: 0;
  }

  .signal-dot-branch {
    background: var(--success);
    box-shadow: 0 0 12px rgb(135 214 163 / 40%);
  }

  .signal-label {
    font-family: var(--font-ui);
    font-size: 0.75rem;
    font-weight: 500;
    color: var(--fg-main);
    line-height: 1.35;
    letter-spacing: 0.02em;
  }

  .signal-node-mono .signal-label {
    color: var(--accent);
  }

  .signal-sublabel {
    font-family: var(--font-ui);
    font-size: 0.625rem;
    font-weight: 500;
    color: var(--fg-muted);
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }

  .signal-connector {
    display: none;
  }

  .signal-branch {
    display: flex;
    flex-direction: column;
    align-items: center;
    order: 3;
  }

  .signal-node[data-col='1'] { order: 1; }
  .signal-node[data-col='3'] { order: 2; }
  .signal-node[data-col='5'] { order: 4; }
  .signal-node[data-col='7'] { order: 5; }
  .signal-node[data-col='9'] { order: 6; }

  .signal-branch-line {
    width: 2px;
    height: 1.5rem;
    background: linear-gradient(180deg, var(--success), rgb(135 214 163 / 20%));
    margin-bottom: var(--space-2);
  }

  .signal-node-branch {
    padding: var(--space-3) var(--space-6);
    border: 1px dashed rgb(135 214 163 / 35%);
    border-radius: var(--radius-md);
    background: rgb(135 214 163 / 5%);
  }

  @media (min-width: 768px) {
    .signal-grid {
      display: grid;
      grid-template-columns: auto 2rem auto 2rem auto 2rem auto 2rem auto;
      grid-template-rows: auto auto;
      justify-content: center;
      column-gap: var(--space-2);
      row-gap: var(--space-2);
      width: max-content;
      max-width: none;
      margin-inline: auto;
    }

    .signal-node,
    .signal-connector {
      order: unset;
    }

    .signal-node[data-col='1'] { grid-column: 1; grid-row: 1; }
    .signal-connector[data-col='2'] { grid-column: 2; grid-row: 1; }
    .signal-node[data-col='3'] { grid-column: 3; grid-row: 1; }
    .signal-connector[data-col='4'] { grid-column: 4; grid-row: 1; }
    .signal-node[data-col='5'] { grid-column: 5; grid-row: 1; }
    .signal-connector[data-col='6'] { grid-column: 6; grid-row: 1; }
    .signal-node[data-col='7'] { grid-column: 7; grid-row: 1; }
    .signal-connector[data-col='8'] { grid-column: 8; grid-row: 1; }
    .signal-node[data-col='9'] { grid-column: 9; grid-row: 1; }

    .signal-branch {
      grid-column: 3;
      grid-row: 2;
      order: unset;
    }

    .signal-connector {
      display: block;
      width: 2rem;
      height: 2px;
      align-self: center;
      background: linear-gradient(90deg, var(--accent), rgb(176 220 232 / 30%));
      position: relative;
    }

    .signal-connector::after {
      content: "";
      position: absolute;
      right: -1px;
      top: 50%;
      transform: translateY(-50%);
      border: 4px solid transparent;
      border-left-color: var(--accent);
    }
  }

  @media (prefers-reduced-motion: no-preference) and (min-width: 768px) {
    .signal-connector {
      background-size: 200% 100%;
      animation: flow 2.5s linear infinite;
    }
  }

  @keyframes flow {
    0% {
      background-position: 100% 0;
    }
    100% {
      background-position: -100% 0;
    }
  }
</style>
